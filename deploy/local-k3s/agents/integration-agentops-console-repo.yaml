apiVersion: agents.agentops.io/v1alpha1
kind: Integration
metadata:
  name: agentops-console-repo
  namespace: agents
spec:
  kind: github-repo
  displayName: AgentOps Console
  description: "Web console — Go BFF + SolidJS PWA, FEP/SSE streaming, agent management UI"
  credentials:
    name: github-token
    key: GITHUB_TOKEN
  github:
    owner: samyn92
    repo: agentops-console
    defaultBranch: main
