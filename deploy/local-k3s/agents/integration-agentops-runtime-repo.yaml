apiVersion: agents.agentops.io/v1alpha1
kind: Integration
metadata:
  name: agentops-runtime-repo
  namespace: agents
spec:
  kind: github-repo
  displayName: AgentOps Runtime
  description: "Fantasy SDK agent runtime — Go binary powering agent pods, memory integration, MCP tools"
  credentials:
    name: github-token
    key: GITHUB_TOKEN
  github:
    owner: samyn92
    repo: agentops-runtime
    defaultBranch: main
