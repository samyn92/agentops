apiVersion: agents.agentops.io/v1alpha1
kind: Integration
metadata:
  name: agentops-platform-repo
  namespace: agents
spec:
  kind: github-repo
  displayName: AgentOps Platform
  description: "Umbrella Helm chart for AgentOps platform — operator, console, memory deployments"
  credentials:
    name: github-token
    key: GITHUB_TOKEN
  github:
    owner: samyn92
    repo: agentops-platform
    defaultBranch: main
