apiVersion: agents.agentops.io/v1alpha1
kind: Integration
metadata:
  name: agentops-memory-repo
  namespace: agents
spec:
  kind: github-repo
  displayName: AgentOps Memory
  description: "Memory service — Go, SQLite + FTS5 BM25 relevance-ranked context injection"
  credentials:
    name: github-token
    key: GITHUB_TOKEN
  github:
    owner: samyn92
    repo: agentops-memory
    defaultBranch: main
