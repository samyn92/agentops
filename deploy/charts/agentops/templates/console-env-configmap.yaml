{{- if (index .Values "agentops-console" "enabled") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: agentops-console-env
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agentops.labels" . | nindent 4 }}
    app.kubernetes.io/component: console
data:
  {{- if .Values.tempo.enabled }}
  TEMPO_URL: {{ include "agentops.tempo.url" . | quote }}
  OTEL_EXPORTER_OTLP_ENDPOINT: {{ include "agentops.tempo.otlpEndpoint" . | quote }}
  {{- end }}
  {{- if .Values.memory.enabled }}
  ENGRAM_URL_OVERRIDE: {{ include "agentops.memory.url" . | quote }}
  {{- end }}
  {{- if .Values.nats.enabled }}
  NATS_URL: {{ include "agentops.nats.url" . | quote }}
  {{- end }}
{{- end }}
